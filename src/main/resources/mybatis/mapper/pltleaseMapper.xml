<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.frd.mapper.pltlease">
    <!--查询-->
	<select id="selectOne" resultType="com.frd.model.pltlease">
		select * from plt_lease where rowid=#{rowid}
	</select>
    <select id="selectList" parameterType="com.frd.model.pltlease"  resultType="com.frd.model.pltlease">
		select * from plt_lease where
        pltname=#{pltname} and
        plttype=#{plttype} and
        qty=#{qty} and
        customer=#{customer} and
        leasedt=#{leasedt} and
        edt=#{edt} and
        adt=#{adt} and
        backqty=#{backqty} and
        remark=#{remark} and
        creator=#{creator} and
        createdt=#{createdt} and
        modifyby=#{modifyby} and
        modifydt=#{modifydt}
	</select>
     <!--新增-->
	<insert id="add" parameterType="com.frd.model.pltlease" >
		insert plt_lease （
        pltname,
        plttype,
        qty,
        customer,
        leasedt,
        edt,
        adt,
        backqty,
        remark,
        creator,
        createdt,
        modifyby,
        modifydt)
        values(
        #{pltname},
        #{plttype},
        #{qty},
        #{customer},
        #{leasedt},
        #{edt},
        #{adt},
        #{backqty},
        #{remark},
        #{creator},
        #{createdt},
        #{modifyby},
        #{modifydt})
	</insert>
	 <!--修改-->
	<update id="update">
		update plt_lease 
        set 
        pltname=#{pltname},
        plttype=#{plttype},
        qty=#{qty},
        customer=#{customer},
        leasedt=#{leasedt},
        edt=#{edt},
        adt=#{adt},
        backqty=#{backqty},
        remark=#{remark},
        creator=#{creator},
        createdt=#{createdt},
        modifyby=#{modifyby},
        modifydt=#{modifydt}
        where rowid=#{rowid}
	</update>
    <delete id="delete">
		delete plt_lease 
        where rowid=#{rowid}
	</delete>
</mapper>