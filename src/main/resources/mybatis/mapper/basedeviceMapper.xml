<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.frd.mapper.basedevice">
    <!--查询-->
	<select id="selectOne" resultType="com.frd.model.basedevice">
		select * from base_device where rowid=#{rowid}
	</select>
    <select id="selectList" parameterType="com.frd.model.basedevice"  resultType="com.frd.model.basedevice">
		select * from base_device where
        devicecode=#{devicecode} and
        devicename=#{devicename} and
        devicetype=#{devicetype} and
        standard=#{standard} and
        status=#{status} and
        creator=#{creator} and
        createdt=#{createdt} and
        modifyby=#{modifyby} and
        modifydt=#{modifydt}
	</select>
     <!--新增-->
	<insert id="add" parameterType="com.frd.model.basedevice" >
		insert base_device （
        devicecode,
        devicename,
        devicetype,
        standard,
        status,
        creator,
        createdt,
        modifyby,
        modifydt)
        values(
        #{devicecode},
        #{devicename},
        #{devicetype},
        #{standard},
        #{status},
        #{creator},
        #{createdt},
        #{modifyby},
        #{modifydt})
	</insert>
	 <!--修改-->
	<update id="update">
		update base_device 
        set 
        devicecode=#{devicecode},
        devicename=#{devicename},
        devicetype=#{devicetype},
        standard=#{standard},
        status=#{status},
        creator=#{creator},
        createdt=#{createdt},
        modifyby=#{modifyby},
        modifydt=#{modifydt}
        where rowid=#{rowid}
	</update>
    <delete id="delete">
		delete base_device 
        where rowid=#{rowid}
	</delete>
</mapper>