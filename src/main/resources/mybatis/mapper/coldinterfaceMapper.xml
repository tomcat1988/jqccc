<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.frd.mapper.coldinterface">
    <!--查询-->
	<select id="selectOne" resultType="com.frd.model.coldinterface">
		select * from cold_interface where rowid=#{rowid}
	</select>
    <select id="selectList" parameterType="com.frd.model.coldinterface"  resultType="com.frd.model.coldinterface">
		select * from cold_interface where
        infcode=#{infcode} and
        infname=#{infname} and
        inftype=#{inftype} and
        status=#{status} and
        param=#{param} and
        url=#{url} and
        infmethod=#{infmethod} and
        infinterval=#{infinterval} and
        remark=#{remark} and
        creator=#{creator} and
        createdt=#{createdt} and
        modifyby=#{modifyby} and
        modifydt=#{modifydt}
	</select>
     <!--新增-->
	<insert id="add" parameterType="com.frd.model.coldinterface" >
		insert cold_interface （
        infcode,
        infname,
        inftype,
        status,
        param,
        url,
        infmethod,
        infinterval,
        remark,
        creator,
        createdt,
        modifyby,
        modifydt)
        values(
        #{infcode},
        #{infname},
        #{inftype},
        #{status},
        #{param},
        #{url},
        #{infmethod},
        #{infinterval},
        #{remark},
        #{creator},
        #{createdt},
        #{modifyby},
        #{modifydt})
	</insert>
	 <!--修改-->
	<update id="update">
		update cold_interface 
        set 
        infcode=#{infcode},
        infname=#{infname},
        inftype=#{inftype},
        status=#{status},
        param=#{param},
        url=#{url},
        infmethod=#{infmethod},
        infinterval=#{infinterval},
        remark=#{remark},
        creator=#{creator},
        createdt=#{createdt},
        modifyby=#{modifyby},
        modifydt=#{modifydt}
        where rowid=#{rowid}
	</update>
    <delete id="delete">
		delete cold_interface 
        where rowid=#{rowid}
	</delete>
</mapper>